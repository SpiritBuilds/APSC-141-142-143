#include <stdio.h>
#include <string.h>
#include <math.h>

int main(void) {

    char string1[11], string2[11];
    printf("Enter string1: ");
    scanf("%s", string1);
    printf("Enter string2: ");
    scanf("%s", string2);

    for (int i = 0; i < strlen(string1); i++) {

    }

    return 0;
}
